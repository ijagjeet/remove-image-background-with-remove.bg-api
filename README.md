# Remove image background with remove.bg API
- Api link [Remove BG API](https://remove.bg)
## Installation
- Install python3 as per platform requirements
- Install packages `pip install requirements`

## Usage
- Run command `python main.py`
